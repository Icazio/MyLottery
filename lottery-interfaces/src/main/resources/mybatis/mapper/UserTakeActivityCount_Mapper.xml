<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.icarus.lottery.infrastructure.dao.IUserTakeActivityCountDao">

    <resultMap id="userTakeActivityCountMap" type="cn.icarus.lottery.infrastructure.po.UserTakeActivityCount">
        <id column="id" property="id"/>
        <result column="u_Id" property="uId"/>
        <result column="activity_id" property="activityId"/>
        <result column="total_count" property="totalCount"/>
        <result column="left_count" property="leftCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.icarus.lottery.infrastructure.po.UserTakeActivityCount">
        INSERT INTO user_take_activity_count(
        u_Id,activity_id,total_count,left_count,create_time,update_time
        )VALUES (
        #{uId},#{activityId},#{totalCount},#{leftCount},now(),now()
        )
    </insert>

    <update id="updateLeftCount" parameterType="cn.icarus.lottery.infrastructure.po.UserTakeActivityCount">
        UPDATE user_take_activity_count
        set left_count=left_count-1,update_time=now()
        where u_id=#{uId} AND activity_id=#{activityId} AND left_count>0
    </update>

    <select id="queryUserTakeActivityCount" parameterType="cn.icarus.lottery.infrastructure.po.UserTakeActivityCount" resultMap="userTakeActivityCountMap">
        SELECT total_count,left_count
        FROM user_take_activity_count
        where u_id=#{uId} and  activity_id=#{activityId}
    </select>



</mapper>