<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.icarus.lottery.infrastructure.dao.IRuleTreeDao">
    <resultMap id="ruleTreeMap" type="cn.icarus.lottery.infrastructure.po.RuleTree">
        <id column="id" property="treeId"/>
        <result column="tree_name" property="treeName"/>
        <result column="tree_desc" property="treeDesc"/>
        <result column="tree_root_node_id" property="treeRootNodeId"/>
    </resultMap>


    <select id="queryRuleTree" resultMap="ruleTreeMap">
        select id,tree_name,tree_desc,tree_root_node_id
        from rule_tree
        where id=#{treeId}
    </select>

</mapper>